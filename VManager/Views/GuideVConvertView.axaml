<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:VManager.ViewModels"
             x:Class="VManager.Views.GuideVConvertView"
             x:DataType="vm:AcercaDeViewModel">

    <StackPanel Spacing="16">

        <TextBlock Text="{Binding L[Guide.VConvert.Title]}"
                   FontSize="22"
                   FontWeight="Bold"
                   TextWrapping="Wrap"/>

        <TextBlock Text="{Binding L[Guide.VConvert.Description]}"
                   TextWrapping="Wrap"
                   FontSize="15"/>

        <StackPanel Spacing="8">
            <TextBlock Text="{Binding L[Guide.VConvert.HowTo.Title]}"
                       TextWrapping="Wrap"
                       FontSize="16"
                       FontWeight="SemiBold"/>

            <TextBlock Text="{Binding L[Guide.VConvert.HowTo.Step1]}" TextWrapping="Wrap" FontSize="14"/>
            <TextBlock Text="{Binding L[Guide.VConvert.HowTo.Step2]}" TextWrapping="Wrap" FontSize="14"/>
            <TextBlock Text="{Binding L[Guide.VConvert.HowTo.Step3]}" TextWrapping="Wrap" FontSize="14"/>
            <TextBlock Text="{Binding L[Guide.VConvert.HowTo.Step4]}" TextWrapping="Wrap" FontSize="14"/>
            <TextBlock Text="{Binding L[Guide.VConvert.HowTo.Step5]}" TextWrapping="Wrap" FontSize="14"/>
        </StackPanel>

        <!-- GIF acá -->
        <!-- <Image Source="/Assets/guides/vconvert.gif" Stretch="Uniform"/> -->

        <StackPanel Spacing="8">
            <TextBlock Text="{Binding L[Guide.VConvert.Notes.Title]}"
                       FontSize="16"
                       FontWeight="SemiBold"/>

            <TextBlock Text="{Binding L[Guide.VConvert.Notes.A]}" TextWrapping="Wrap" FontSize="14"/>
            <TextBlock Text="{Binding L[Guide.VConvert.Notes.B]}" TextWrapping="Wrap" FontSize="14"/>
        </StackPanel>

    </StackPanel>
</UserControl>